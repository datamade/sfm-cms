setup:
  addons:
    - plan: heroku-postgresql:hobby-basic
    - plan: websolr:staging-20
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./scripts/release.sh
  image: web
run:
  web: gunicorn -t 180 --log-level debug sfm_pc.wsgi:application
